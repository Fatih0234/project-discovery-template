# GitHub token — used for both the GitHub search API and GitHub Models (LLM summarization)
# Locally: set GITHUB_TOKEN
# In Codespaces: set GH_TOKEN (GitHub blocks the GITHUB_ prefix in Codespaces secrets)
GITHUB_TOKEN=ghp_your_token_here
